while(x > 3) do
    begin
        if(x == 3) then write(x) else x := x + 1;
        if(x < 3) then
            begin
                y := 8;
                while(y > 3) do y := y - 1
            end
        else
            while(z <= 3) do
                begin
                    z := 1;
                    x := 2;
                    y := z * x - 1
                end;
            d := 2 + 2 * 2;
            read(x);
            write(d);
        if(x > 3) then
            write(x);
            x := x ** 2
        else
            x := x / 2;
            y := y + 2 * 2

        (* comment 1
            (* comment 1.1
                (* comment 1.1.1 *)
            *)
            (* comment 1.2
                (* comment 1.2.1*)
            *)
            (* comment 1.3
                (* comment 1.3.1
                    (*comment 1.3.1.1*)
                    (*comment 1.3.1.2*)
                *)
            *)
        *)
    end
